# ── envs / caches ──────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
.streamlit/

# ── large training corpora + token-id caches (regenerated by fetch_data.py / build_zoo.py) ──
pilot/data/
pilot/data_bio/
pilot/data_dom/
pilot/data_gen/
pilot/_quick_archive/
pilot/*.log

# ── retrain backups + temp swap dirs (safe locally; must NOT ship) ──────────────
models_v2_old/
models_v2_new/
pilot/rag_dom_old/
pilot/rag_dom_new/

# ── v1 / pilot artifacts — the deployed repo ships ONLY the v2 dashboard (app_v2.py) ──
# old dashboard, its model zoo, and its RAG index:
app.py
models/
pilot/rag/
# pilot experiment outputs + scripts (not used by app_v2.py or build_zoo.py):
pilot/RESULTS.md
pilot/models/
pilot/results/
pilot/probe_terms.txt
src/analyze.py
src/fertility.py
src/build_model.py
src/train_tokenizer.py
docs/
# helper/orchestration scripts (contain local paths; not needed to run or deploy the app):
run_full.sh
zoo.sh
run_zoo_v3.sh

# ── scratch ─────────────────────────────────────────────────────────────────────
*.tmp

# KEPT (runtime for app_v2.py): app_v2.py; src/{bpe,model,glass,audit,rag,ui,ui2,biomarker,
# train,fetch_data,warm_tokenizers,build_zoo}.py; models_v2/*.pt; pilot/tokenizers/*.json;
# pilot/rag_dom/*; assets/**; content/*; tests/*; requirements*.txt; README.md; DEPLOY.md
